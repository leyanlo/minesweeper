import React from 'react';

import { Hero, HeroCard } from '../components/styled/hero';
import Layout from '../components/layout';

export default () => (
  <Layout>
    <Hero>
      <HeroCard>Minesweeper</HeroCard>
    </Hero>
  </Layout>
);
